[phases.setup]
nixPkgs = ["python311", "gcc", "libffi"]

[phases.install]
cmds = ["pip install -r requirements.txt --extra-index-url https://download.pytorch.org/whl/cpu"]

[start]
cmd = "uvicorn backend.main:app --host 0.0.0.0 --port $PORT"
